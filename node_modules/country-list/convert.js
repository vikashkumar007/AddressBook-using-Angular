'use strict'

var data = require('./data.json');

/**
 * Precompute name and code lookups.
 */
var nd = [];
data.forEach(function(country) {
  /*
  nd.push({
    code: country.Code,
    name: country.Name
  });*/

  console.log('  { "code": "%s", "name": "%s" },', country.Code, country.Name);
});

//console.log(JSON.stringify(nd));
